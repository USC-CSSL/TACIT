package edu.usc.cssl.nlputils.application.utility;


public class ImageIcon {//extends AbstractUIPlugin {

/*	@Override
	public ImageRegistry getImageRegistry() {

		ImageRegistry registry = super.getImageRegistry();
		Bundle bundle = Platform.getBundle(NlpUtilsProductConstant.PLUGIN_ID);
		ImageDescriptor backImage = ImageDescriptor.createFromURL(FileLocator
				.find(bundle, new Path("icons/backward_nav.gif"), null));
		registry.put(NlpUtilsProductConstant.IMAGE_BACK, backImage);
		ImageDescriptor stepImage = ImageDescriptor.createFromURL(FileLocator
				.find(bundle, new Path("icons/step_current.gif"), null));
		registry.put(NlpUtilsProductConstant.IMAGE_FRONT, stepImage);
		return registry;
	}*/
}
